Task: Add failing policy and canonical-discovery coverage for URL preview images\n\nSummary: Added synthetic link-unfurl coverage proving canonical cache normalization retains the image, public third-party preview images must stay on the direct WebView path, and private Slack preview images must remain native asset requests after raw attachments are discarded.\n\nModified: src/slack_message_wire.rs; src/window.rs\n\nRed verification: cargo test link_unfurl_image ran 3 tests; the canonical parser guard passed, while public URL policy and cached private-image discovery failed exactly as expected.\n\nRationale: Reproduce both causes without credentials or private workspace content.
