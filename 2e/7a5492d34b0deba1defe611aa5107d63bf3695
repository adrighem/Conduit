Task: Add failing coverage for image blocks nested inside legacy attachments. The regression reproduces the exact allowlisted Slack response shape observed for the reported GIF: an attachment with id, fallback, and one animated image block. It requires canonical normalization to retain the image URL, alt text, and title without leaving a fallback-only attachment. Red verification: cargo test attachment_embedded_image_block_survives_canonical_normalization failed because the embedded blocks array was discarded.
