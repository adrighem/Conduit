# PR:18 - Release 0.3.0

- Status: open; defer merge
- Confidence: high
- Provenance: regenerated Release Please head `332addb` based directly on `f0fe9ec`
- Diff: six expected release metadata files only; versions are synchronized at 0.3.0 and there are no workflow, dependency, build, install, permission, binary, or credential changes
- Validation: exact-head CodeQL `30448699855` and replacement CI `30450146868` pass; GitHub reports the PR mergeable
- Runner anomaly: initial CI `30448709555` was cancelled after its external package install made no progress for 20 minutes; the replacement completed every check in 8 minutes
- Release-note gap: the generated changelog and PR body omit the status emoji search repair from `0b074b2` and the startup interaction plus sent-message animation fix from `982f628`
- Release gate: merging creates the tag and starts automatic package publication, but there is no recorded completion of the 0.3.0 manual pre-tag checklist
- Security follow-up: the regenerated notes include commit `280bec6`, and Dependabot alert 1 is fixed
- Issue linkage: Release Please rendered the `Refs #14` footer as `closes #14`, but ISSUE:14 still lacks the requested clean-build timing evidence
- Next step: update the generated release notes and ISSUE:14 linkage, complete the manual checklist, revalidate the exact head, then request explicit merge approval
- Public action: Release Please regenerated the PR after the approved main push; no comment or merge
