# ISSUE:12 — Bounded runtime and incremental presentation

- Status: open; Phase 6 manually verified and archived; borrowed catalog reads completed in `130266c`; targeted sidebar source updates completed in `80e672f`; realtime persistence admission bounded in `fcf641c`; command admission classified in `95b96a9`; supervised realtime shutdown completed in `65e16fd`; bounded runtime command ingress completed in `7ecc75c`; bounded runtime-to-GTK publication completed in `7c644ab`; bounded huddle actor admission completed in `321eab8`
- Confidence: high
- Impact: P1 native-media transport and GTK-local presentation paths can still amplify bursts
- Intent: bound prioritized work before spawning, coalesce only replaceable synchronization, and update presentation through keyed projections
- Relationship: spans workspace-pipeline Phases 3 and 4 and must consume authoritative coordinator patches from ISSUE:11
- Risks: backpressure must never silently drop durable user actions or read markers; selection and shutdown behavior must survive incremental updates
- Current evidence: Phase 6 removed broad workspace snapshots and compatibility events; sidebar GTK mutations are keyed and virtualized; ordinary presentation scans borrow the conversation catalog; presentation-only unread patches now build only changed rows and replace indexed visible positions in O(changed rows), while structural and membership cases fall back to a full rebuild; realtime persistence now has an awaited FIFO capacity of 256, keeps queue metrics inside that bound, withholds Socket Mode ACKs on admission timeout or rejection, preserves order, and drains before normal reconnect; all 42 runtime commands declare control, durable-action, read-marker, coalescible, or supersedable admission with opaque replacement keys; command ingress now uses bounded per-policy queues, fixed lane drivers, task-admission and execution limits, same-key coalescing, ordered durable/read reservations, fair ready dispatch, explicit GTK rejection recovery, session-start ordering, and EOF drain; runtime-to-GTK publication now uses a cancellation-safe capacity-256 FIFO mailbox with ticket-fair reliable blocking, a 32-slot typed progress budget, same-key progress replacement, terminal eviction, closure wakeups, EOF drain, and bounded metrics; the huddle coordinator now uses a bounded FIFO actor with capacity 64, limits observations to 56 queued items, reserves eight positions for commands, awaits Socket Mode admission before ACK, completes commands after effect cascades, and drains realtime before huddle reset and shutdown; admission metrics remain inside physical queue bounds under concurrent dequeue and replacement admission; 1,051 default tests pass with 3 ignored, strict all-target and all-feature Clippy passes, and all 17 serial Meson suites pass at `321eab8`
- Next step: implement bounded native-media transport admission and teardown, then GTK-local amplification
- Public action: implementation commits pushed through the normal mainline workflow; no issue comments, labels, or closures
