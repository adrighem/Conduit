Task: Add failing coverage for the selected custom emoji preview\n\nSummary: Added a headless custom-status case whose picker remains closed, asserts the selected summary is a custom image, and requires two animated frame updates.\n\nModified: src/window.rs; tests/test_status_dialog.py\n\nRed verification: status-ui failed after 22.45s because the selected late_status_parrot summary reported text instead of custom-image. The closed picker and empty grid isolate the failure to the selected summary.\n\nRationale: Prevent text-only shortcode fallback from hiding a selected custom emoji.
