Task: Render selected custom emoji images and animation beside status text\n\nSummary: Added a dedicated native selected-emoji preview to the Set Status row. Picker activation now preserves the full typed emoji result, Unicode selections render as glyphs, custom selections reuse the animated image loader, and unresolved shortcodes retain a text fallback. Late workspace-catalog refreshes upgrade an unresolved selection to its image.\n\nModified: src/window.rs; tests/test_status_dialog.py\n\nVerification: cargo fmt --check and cargo check passed; cargo test passed 891 tests with 3 ignored; all 17 Meson suites passed; the focused closed-picker status test observed at least two selected-preview animation frames. Local clippy is unavailable because the installed clippy driver is older than the project compiler requirement; exact CI lint remains required.\n\nRationale: A text-only ActionRow subtitle cannot display a custom emoji. A separate preview widget preserves image and GIF rendering while Slack submission remains shortcode-only.
