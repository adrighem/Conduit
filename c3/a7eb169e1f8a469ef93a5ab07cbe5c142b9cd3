Task: Preserve public direct preview images and discover canonical private images\n\nSummary: Shared the native preview URL allowlist between discovery and download, left third-party HTTP(S) unfurl images on the existing direct WebView path, and added canonical image discovery for attachment, image-block, and section-accessory images after cache normalization. Slack credentials remain restricted to validated Slack HTTPS hosts.\n\nModified: src/slack.rs; src/rich_message.rs; src/window.rs; src/message_html.rs\n\nVerification: cargo fmt --check and cargo check passed; 896 Rust tests passed with 3 ignored; Meson compilation passed; focused parser, URL-policy, canonical-discovery, and renderer tests passed. Full Meson validation follows after hardening the repeated Xvfb activation race.\n\nRationale: Prevent an expected third-party download rejection from overwriting WebKit's valid public image, while retaining authenticated private Slack preview loading from canonical cached messages.
