# PR:18 - Release 0.3.0

- Status: open; defer merge
- Confidence: high
- Provenance: regenerated Release Please head `41107f0` based directly on `bbf9539`
- Diff: six expected release metadata files only; versions are synchronized at 0.3.0 and there are no workflow, dependency, build, install, permission, binary, or credential changes
- Validation: refreshed-head CI `30468524296` and CodeQL `30468505063` pass; GitHub reports the PR mergeable
- Release-note gap: the generated changelog and PR body omit the status emoji search repair from `0b074b2` and the startup interaction plus sent-message animation fix from `982f628`
- Included follow-up: the regenerated notes now include send responsiveness commit `bbf9539`
- Release gate: merging creates the tag and starts automatic package publication, but there is no recorded completion of the 0.3.0 manual pre-tag checklist
- Security follow-up: the regenerated notes include commit `280bec6`, and Dependabot alert 1 is fixed
- Issue linkage: Release Please still renders the `Refs #14` footer as `closes #14`; ISSUE:14 is already closed independently, so attribution should remain a non-closing reference
- Release measurements: ISSUE:14 controlled clean builds and three exact-head attention runs are complete and recorded
- Next step: finish the last main documentation push, update the generated release notes and linkage as the final PR content change, validate that exact head, then wait for live checklist confirmation
- Public action: Release Please regenerated the PR after the send-responsiveness push; no comment or merge
